Algoritmo "tabuada"

procedimento tabuada(numero : inteiro)
var
   i, multiplicacao : inteiro
inicio
      para i de 0 ate 10 passo 1 faca
            multiplicacao <- numero * i
           escreval(numero , " X" , i , " =" , multiplicacao)
      fimpara
fimprocedimento

Var

n : inteiro

Inicio

      escreva("Voce quer a tabuada de qual numero?")
      leia(n)
      tabuada(n)

Fimalgoritmo
